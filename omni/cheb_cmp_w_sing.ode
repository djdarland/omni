diff ( y , x , 1 ) = exp( m1 * x ) * expt ( cos ( 6.0 * x ) , 5.0 ) * expt ( sin ( 5.0 * x ) , 6.0 ) ;
!
Digits := 32;
max_terms := 30;
!
x_start := -1.0;
x_end := 1.0 ;
diff(y,0,0.0);
glob_look_poles := true;
glob_max_iter := 1000000;
!
exact_soln_y := proc(x)
return(0.0);
end;
