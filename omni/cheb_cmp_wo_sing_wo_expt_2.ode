diff ( y , x , 1 ) =  exp( m1 * x ) * cos ( 6.0 * x ) * cos ( 6.0 * x ) * cos ( 6.0 * x ) * cos ( 6.0 * x ) * cos ( 6.0 * x ) *  sin ( 5.0 * x ) *  sin ( 5.0 * x ) *  sin ( 5.0 * x ) *  sin ( 5.0 * x ) *  sin ( 5.0 * x ) *  sin (5.0* x ) ; 
!
Digits := 32;
max_terms := 30;
!
x_start := -1.0;
x_end := 1.0 ;
diff(y,0,0.0);
glob_look_poles := false;
glob_max_iter := 1000000;
glob_max_h := 0.001;
!
exact_soln_y := proc(x)
return(0.0);
end;
